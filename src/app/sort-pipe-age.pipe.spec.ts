import { SortPipeAgePipe } from './sort-pipe-age.pipe';

describe('SortPipeAgePipe', () => {
  it('create an instance', () => {
    const pipe = new SortPipeAgePipe();
    expect(pipe).toBeTruthy();
  });
});
