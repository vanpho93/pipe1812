import { SortStudentPipe } from './sort-student.pipe';

describe('SortStudentPipe', () => {
  it('create an instance', () => {
    const pipe = new SortStudentPipe();
    expect(pipe).toBeTruthy();
  });
});
