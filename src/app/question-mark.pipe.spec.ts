import { QuestionMarkPipe } from './question-mark.pipe';

describe('QuestionMarkPipe', () => {
  it('create an instance', () => {
    const pipe = new QuestionMarkPipe();
    expect(pipe).toBeTruthy();
  });
});
